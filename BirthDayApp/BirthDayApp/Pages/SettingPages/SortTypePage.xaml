<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:locale="clr-namespace:BirthDayApp.Items"
             x:Class="BirthDayApp.Pages.SettingPages.SortTypePage"
             Title="СОРТИРОВКА">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="button" TargetType="Button" x:Name="styleButton">
                <Setter Property="BackgroundColor" Value="SlateBlue"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="Margin" Value="30,5"/>
                <Setter Property="CornerRadius" Value="15"/>
                <Setter Property="BorderColor" Value="SlateBlue"/>
                <Setter Property="BorderWidth" Value="2"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Padding="0,20" x:Name="stackButtons">
            <Button Text="По умолчания" Style="{StaticResource button}" Clicked="Button_Clicked" x:Name="defaultSort"/>
            <Button Text="По алфавиту (Возрастание)" Style="{StaticResource button}" Clicked="Button_Clicked" x:Name="abc_a_z"/>
            <Button Text="По алфавиту (Убывание)" Style="{StaticResource button}" Clicked="Button_Clicked" x:Name="abc_z_a"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>